{
  "name": "fsx",
  "version": "1.9.0",
  "main": "js/index",
  "scripts": {
    "build": "coffee-build src -o js",
    "prepublish": "prepv -i || true"
  },
  "devDependencies": {
    "coffeescript": "^2.2.3",
    "wch-coffee": "^0.1.1"
  }
}
